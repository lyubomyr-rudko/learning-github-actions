name: 08 - Using Variables
on:
  push:
  workflow_dispatch:
env:
  MY_NAME: "Bob"
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: |
          echo "hello $MY_NAME"
